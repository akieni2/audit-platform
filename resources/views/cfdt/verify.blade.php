<!doctype html><html><body><main style="max-width:700px;margin:60px auto;font-family:Arial"><h1>{{$valid?'Certificat CFDT authentique':'Certificat CFDT non valide'}}</h1>@if($valid)<p><strong>{{$certificate->number}}</strong></p><p>{{$certificate->enrollment->user->prenom}} {{$certificate->enrollment->user->name}} — {{$certificate->enrollment->course->title}}</p><p>Score {{$certificate->score}} % · {{$certificate->issued_at->format('d/m/Y')}}</p>@else<p>L'intégrité de ce certificat n'a pas pu être confirmée.</p>@endif</main></body></html>
