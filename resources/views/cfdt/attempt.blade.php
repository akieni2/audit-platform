<x-app-layout><form class="mx-auto max-w-4xl space-y-5" method="post" action="{{route('cfdt.submit',$course)}}">@csrf<h1 class="dgcpt-page-title">Évaluation — {{$course->title}}</h1>@foreach($course->questions??[] as $q)<fieldset class="dgcpt-surface p-5"><legend class="font-bold">{{$loop->iteration}}. {{$q['text']}}</legend>@foreach($q['options'] as $i=>$o)<label class="mt-3 block"><input type="{{$q['type']==='multiple'?'checkbox':'radio'}}" name="answers[{{$q['id']}}][]" value="{{$i}}"> {{$o}}</label>@endforeach</fieldset>@endforeach<button class="dgcpt-btn-primary">Soumettre définitivement</button></form></x-app-layout>
